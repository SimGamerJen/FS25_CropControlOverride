# Alpha 18.3.1 Layout Hotfix

Alpha 18.3 attempted to place suitability/source text beneath every crop name.
The calendar rows are only 34px high, so that second line overlapped the next row.

This hotfix restores the clean Alpha 18.2 single-line row layout.

The useful Alpha 18.3 diagnostics remain:
- exact fallback crop names and reasons in Regional Preview;
- crop-level regional/fallback diagnostics in log.txt.

The regional Apply -> forced NPC regeneration -> contract rebuild transaction is unchanged.
